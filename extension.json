
{
  "id": "layout-default-field-value",
  "name": "Default value for the layout field",
  "srcdoc": "extension.html",
  "fieldTypes": ["Symbol", "Symbols"],
  "parameters": {
    "instance": [
      {
        "id": "defaultLayout",
        "name": "Default layout",
        "description": "Set which layout is the default",
        "type": "Enum",
        "options": [{"container-fluid": "container-fluid"}, {"container": "container"}, {"eight-column": "eight-column"}],
        "default": "container",
        "labels": {"empty": "Choose a layout"},
        "required": true
      }
    ]
  }
}
